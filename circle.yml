machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t masakura/app .

deployment:
  hub:
    branch: master
    command:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push masakura/app